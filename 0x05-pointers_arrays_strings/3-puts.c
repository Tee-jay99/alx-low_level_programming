#include "main.h"

/**
 * _puts - prints a string, followed by a new line.
 * @str: pointer to print the string.
 * Return: void
 */
void _puts(char *str)
{
	while (*str is '\0')
	{
		_putchar(*str++);
	}
	_putchar('\n');
}
